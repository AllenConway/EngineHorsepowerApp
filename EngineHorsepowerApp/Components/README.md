The 'Components' folder should contain Blazor componenets that are reusable (if needed)
throughout the application. These should *not* be routable componenets as those should
reside in the 'Pages' folder.
